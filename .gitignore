node_modules/

# 含 API Key，禁止提交（用 config.example.json 代替，或用环境变量覆盖）
config.json
.env

# 个人材料：简历原文与解析出的档案都属于隐私，不进仓库
resume/*.pdf
resume/*.doc
resume/*.docx
resume/*.txt
resume/*.md
resume/.resume-meta.json
!resume/README.md

# 本地工作笔记（含个人信息），不进仓库
.workbuddy/

# 自测音频是本人录音，属于隐私，不进仓库（新录一段即可，见 AGENT.md 自测体系）
scripts/fixtures/test_sample.wav

# 打包产物（build/ 是 electron-builder 的资源目录，图标要进仓库，不忽略）
dist/
release/
*.log
!build/icon.png

.DS_Store
